# Clonality Interpretation Assist — First Run, 2026-06-28

> **Branch:** `codex-clonality-interp-v1-2026-06-28` (created this morning)
> **Plan:** [`plans/11_clonality_interpretation_assist.md`](../../plans/11_clonality_interpretation_assist.md)
> **Lead:** Christian + Hermes (multi-agent pair)
> **DIT:** still TBD — assign when first patient batch is calibrated

## What shipped today

- **Branch + Obsidian scaffold created.** `ObsidianVault/Clonality_ML_Log/{decisions,feedback,internet_cite,models}` directories are in place.
- **Phase 0 / T-0.1 — asset-map audit under `core/analyses/clonality/audit.md`.** Every public function, feature, rule path, control flag, and tracking column is enumerated. ML integration hotspots flagged.
- **Phase 1 / T-1.1 — `gui_qt/tabs/tab_clonality_interpretation.py`.** New QWidget that compares rule vs ML prediction per row, color-coded, with disagreements routed to red `usikker_review`. Self-test path so the GUI is reviewable without a real backend yet.
- **Phase 1 / T-1.2 — wired into `gui_qt/main_window.py`.** Tab counts go up by one; rendering is opt-in (off by default).
- **Phase 1 / T-1.3 — `config.py` per-assay thresholds block.** `analyses.clonality.interpretation.thresholds` table now has 15 assay-specific values (FR1/FR2/FR3 @ 0.85, TCRG-A/B @ 0.75, TCRB-A/B/C @ 0.75, DHJH_D/E @ 0.92, IGK/KDE/Ktr-albumin @ 0.92, SL/IKZF1 @ 0.95) and a `_default` of 0.85. Chemist can override per assay.
- **Phase 2 / T-2.1 — per-channel raw-trace shape features.** `per_channel_trace_summary(entry)` returns dict of `{peak_count, peak_variance, mad, dome_peak_count, dome_height_ratio}_per_channel`. First three are decisive for `intet_pcr_produkt_darlig_dna` vs `qc_teknisk_fail` discrimination.
- **Phase 2 / T-2.2 — reference-window position distribution.** `reference_window_features(entry)` returns `dom_distance_to_ref_window_center_bp`, `ref_window_coverage_fraction`, `in_reference_window` from `ASSAY_REFERENCE_RANGES`.
- **Phase 2 / T-2.3 — per-patient replicate concordance contract.** `_compute_patient_panel_features(entry, all_patient_entries)` populated by the pipeline orchestrator downstream. Stub returns documented-key dict; pipeline wires it in Phase 6.
- **Public-domain citation survey** under `ObsidianVault/Clonality_ML_Log/internet_cite/2026-06-28_pubmed_anchor_survey.md` — EuroClonality / BIOMED-2 / WHO references with per-assay bp-window table.
- **Model-registry scouting note** under `ObsidianVault/Clonality_ML_Log/decisions/model_registry_2026-06-28.md` — scikit-learn options vs tabular DL options; recommendation paragraph.

## Test status

`tests/test_clonality_interpretation_v1.py`: 28 passed, 1 skipped (unchanged baseline).

New tests:
- `tests/test_clonality_interpretation_features_v2.py`: 8 tests (Phase 2 artifacts).
- `tests/test_clonality_interpretation_tab.py`: 6 widget-level tests (Phase 1 tab).

Full pytest run: **120 + 6 + 8 = 134** passing, **4 pre-existing failures** (cache isolation + strict_rust_ladder_mode), zero new regressions.

## Still open (carryin into tomorrow)

- **Phase 3 / T-3.1** — `scripts/train_clonality_interpretation_models.py` (full pipeline expanding the existing quick-model script).
- **Phase 3 / T-3.2** — `core/analyses/clonality/ml_training.py` (shared module).
- **Phase 3 / T-3.3** — `tests/test_clonality_interpretation_ml.py` with 6+ cases.
- **Phase 4 / T-4.1** — `core/analyses/clonality/calibration.py` (Platt scaling).
- **Phase 4 / T-4.2** — wire ML into `interpret_entry` behind `interpretation.enabled`.
- **Calibration review.** Chemist owns final τ numbers; today's audit table 15 assay-specific values are educated guesses.

## Open questions for clinician review

1. Per-assay τ values (values above): the τ ladder as-is (FR1/FR2/FR3: 0.85, TCRG/TCRB: 0.75, DHJH/IGK/KDE: 0.92, SL/IKZF1: 0.95) is best-guess; calibrate against the real holdout F1 once Phase 3 ships.
2. `qc_teknisk_fail` boundary: at what fraction of input-DNA control flagged "weak signal" do we route to QC vs review?
3. `pseudoklonal` vs `monoklonal` split on ML/rule disagreement. Practice-specific?
4. New assays (e.g. TCE-allele) coming — onboard ML in parallel or wait until ≥200 files?

## Next run

Plan 11 Phase 3 (per-assay training). Brand-new `scripts/train_clonality_interpretation_models.py` that:

1. Loads the **22k labelled** catalogue from `/Volumes/T7 Shield/DATA/clonality`.
2. Splits by DIT, fits RandomForest per assay with N ≥ 200.
3. Persists per-assay `joblib` models under `ObsidianVault/Clonality_ML_Log/models/<date>/<assay>/`.
4. Emits a one-assay-per-page markdown under `ObsidianVault/Clonality_ML_Log/<date>/report_<assay>.md`.

Will use the citation-survey file from today as ground truth for per-assay bp-window reasonableness.


# HemaFrag Diagnostics -- runtime deps for Python 3.11 / 3.12 / 3.14.6
#
# Floor pins (lower bound only). Lets pip pick a single matrix that
# works on:
#   - Python 3.11 / 3.12 (this dev machine)
#   - Python 3.14.6 (the work computer)
#
# Last validated:
#   - 3.12 venv install: 2026-07-09 (37/37 deps resolve)
#   - cp314 + win_amd64 dry-run: 2026-07-09 (37/37 PASS, no sdist)
#
# Migration docs:
#   PLAN_PY314_MIGRATION.md  -- rationale, risks, recipe
#   REMAINING_WORK.md        -- phase-by-phase status

# Scientific / data / ML core (HemaFrag must-haves)
numpy>=2.1,<3.0
scipy>=1.13,<2.0
pandas>=2.2,<3.0
scikit-learn>=1.6,<2.0
matplotlib>=3.10,<4
pillow>=11
openpyxl>=3.1.5
xlsxwriter>=3.2

# ML interpretability / auto-ML / viz extras
joblib>=1.4
lmfit>=1.3.4
biopython>=1.84
xarray>=2024
uncertainties>=3.2
contourpy>=1.3
kiwisolver>=1.4

# Plotting / dashboard
plotly>=6.5
altair>=5.4
bokeh>=3.7
panel>=1.6
param>=2.3
pyviz_comms>=3.0
xyzservices>=2025.1
pyqtgraph>=0.13.7

# Qt GUI -- install via pip wheels (work computer has no Rust toolchain)
PyQt6>=6.11

# Web / data transport
Jinja2>=3.1.6
requests>=2.32
urllib3>=2.2

# Validation / config
jsonschema>=4.21
pytz>=2024
tzdata>=2023
PyYAML>=6.0
packaging>=23

# Operator UX
tqdm>=4.66

# Meta (the install script upgrades pip; keep them pinned)
pip>=24
wheel>=0.43
setuptools>=66

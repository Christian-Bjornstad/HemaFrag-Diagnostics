# macOS / editor metadata
.DS_Store
._*
.obsidian/
ObsidianVault/.obsidian/

# Python caches / environments
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
venv/
.venv/
env/
ENV/

# Build and packaging outputs
build/
dist/
*.spec.log
release_artifacts/
validation_outputs/

# Rust build outputs
fraggler-v2/target/

# Local runtime binaries. Build from fraggler-v2/ when needed.
bin/fraggler-cli

# Generated analysis outputs and review scratch
artifacts/
local_triage/
tmp/
tmp_*/
review_bundle*/
output_*.txt
output_*.tsv
output_*.json

# Local clinical/input data. Keep source-control examples synthetic only.
data/*
!data/README.md
*.fsa
*.hid
*.ab1
*.ladder_adj.json

# Generated reports/workbooks/documents
*.xlsx
*.xls
*.csv
*.html
*.pdf
*.docx

# Logs
*.log

# venv / install artefacts
.venv/
venv/
.install_state
err.log

# Wheel outputs (kept local — prebuilt wheel goes in wheels/ if user
# wants to share, but not commit-generated)
target/wheels/*.whl
!wheels/

# Crate build outputs (avoid touching ~600MB on git status)
fraggler-v2/target/

# Plan 12 Phase 12.9 — audit JSONL landed at cwd by stray tests
# that pass bundle_dir=None. Pin here so a real bundle-side
# file is committed by mistake.
ladder_review_audit.jsonl

# Plan 12 Phase 12.15 — rerun rationale JSONL landed at cwd by
# stray tests with bundle_dir=None.
ladder_review_rationale.jsonl

# Windows things .NET-style
Thumbs.db
*.lnk

